<!-- Generated by scripts/generate_command_reference.py. Do not edit by hand; run `make reference`. -->

# warcraft-wiki

Warcraft Wiki reference CLI.

## Global options

Pass these before the subcommand: `warcraft-wiki --pretty <command> ...`.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--pretty` | boolean | false | Pretty-print JSON for human reading. Default output is compact JSON. |
| `--compact` | boolean | false | Truncate long string fields to reduce payload size. |
| `--fields` | str (repeatable) |  | Return only selected fields (dot paths). Repeat or pass comma-separated values. |
| `--fields-strict` | boolean | false | Fail when a requested --fields dot-path is missing from the payload. |
| `--profile` | str |  | Output profile preset: agent (default compact JSON) or human (pretty JSON). |
| `--compact-max-chars` | int range | 280 | Maximum string length before --compact truncation adds an ellipsis. |

## warcraft-wiki doctor

Report Warcraft Wiki CLI readiness, per-command capabilities, and cache configuration.

## warcraft-wiki search

Search Warcraft Wiki articles by free text, ranked by title match and content family.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | Query text to match against Warcraft Wiki article titles. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int range | 5 | Maximum results to return. |

## warcraft-wiki resolve

Resolve a free-text query to the best matching wiki article plus the follow-up command.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | Free-text query to resolve to the single best Warcraft Wiki article. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int range | 5 | Maximum candidates to inspect. |

## warcraft-wiki article

Fetch one wiki article with extracted text, headings, and previews of navigation and linked entities.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `article_ref` | str | required | Wiki article title or warcraft.wiki.gg URL. |

## warcraft-wiki article-full

Fetch one wiki article with every extracted section and the complete linked-entity list.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `article_ref` | str | required | Wiki article title or warcraft.wiki.gg URL. |

## warcraft-wiki api

Resolve a query to an API, framework, CVar, or XML reference page and return its summary.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | API or programming reference query, article title, or URL. |

## warcraft-wiki api-full

Resolve a query to an API reference page and return every extracted section.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | API or programming reference query, article title, or URL. |

## warcraft-wiki event

Resolve a query to a game event or UI handler reference page and return its summary.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | Game event name (PLAYER_LOGIN), UI handler name, article title, or URL. |

## warcraft-wiki event-full

Resolve a query to a game event or UI handler reference page and return every extracted section.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | Game event name (PLAYER_LOGIN), UI handler name, article title, or URL. |

## warcraft-wiki article-export

Write a wiki article to a local bundle directory for offline querying.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `article_ref` | str | required | Wiki article title or warcraft.wiki.gg URL. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--out` | path |  | Output directory. Defaults to ./warcraft-wiki_exports/article-<slug>. |

## warcraft-wiki article-query

Search an exported wiki article bundle on disk without touching the network.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `bundle` | str | required | Exported bundle directory produced by article-export. |
| `query` | str | required | Query text to match against the exported article bundle. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int range | 5 | Maximum matches to return per kind. |
| `--kind` | str (repeatable) |  | Restrict search kinds. Repeat or pass comma-separated values from: sections, navigation, linked_entities. |
| `--section-title` | str |  | Restrict section matches to a title substring. |
