<!-- Generated by scripts/generate_command_reference.py. Do not edit by hand; run `make reference`. -->

# lorrgs

Lorrgs cooldown timeline and composition ranking CLI.

## Global options

Pass these before the subcommand: `lorrgs --pretty <command> ...`.

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--pretty` | boolean | false | Pretty-print JSON for human reading. Default output is compact JSON. |
| `--compact` | boolean | false | Truncate long string fields to reduce payload size. |
| `--fields` | str (repeatable) |  | Return only selected fields (dot paths). Repeat or pass comma-separated values. |
| `--fields-strict` | boolean | false | Fail when a requested --fields dot-path is missing from the payload. |
| `--profile` | str |  | Output profile preset: agent (default compact JSON), human (pretty JSON), debug (pretty JSON + diagnostics). |
| `--compact-max-chars` | int range | 280 | Maximum string length before --compact truncation adds an ellipsis. |

## lorrgs doctor

Report Lorrgs auth posture, endpoints, and per-surface capability state.

## lorrgs roles

List Lorrgs raid roles.

## lorrgs classes

List Lorrgs player classes.

## lorrgs specs

List Lorrgs specs with their slugs and roles.

## lorrgs search

Search Lorrgs by explicit URL/ref or by spec/boss terms.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | Search Lorrgs routes, Warcraft Logs report refs, or spec/boss text. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int range | 5 | Maximum results to return. |

## lorrgs resolve

Resolve a Lorrgs query conservatively.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `query` | str | required | Resolve a Lorrgs URL/ref or spec/boss query into a next command. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int range | 5 | Maximum candidates to inspect. |

## lorrgs spec

Fetch metadata for one Lorrgs spec.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `spec_slug` | str | required | Lorrgs full spec slug, e.g. mage-frost. |

## lorrgs spec-spells

Fetch the tracked cooldown spells for one spec.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `spec_slug` | str | required | Lorrgs full spec slug, e.g. mage-frost. |

## lorrgs zones

List Lorrgs raid zones.

## lorrgs season

Fetch season-to-raid partition metadata for one season.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `season_slug` | str | current | Lorrgs season slug; use current for the current season. |

## lorrgs current-season

Fetch season-to-raid partition metadata for the current season.

## lorrgs zone

Fetch metadata for one raid zone.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `zone_id` | float | required | Lorrgs/Warcraft Logs zone id. |

## lorrgs zone-bosses

List the bosses in one raid zone.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `zone_id` | float | required | Lorrgs/Warcraft Logs zone id. |

## lorrgs bosses

List every boss Lorrgs tracks.

## lorrgs boss

Fetch metadata for one boss.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `boss_slug` | str | required | Lorrgs boss slug, e.g. chimaerus-the-undreamt-god. |

## lorrgs boss-spells

Fetch the tracked boss abilities for one encounter.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `boss_slug` | str | required | Lorrgs boss slug, e.g. chimaerus-the-undreamt-god. |

## lorrgs spell

Fetch Lorrgs metadata for one spell id.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `spell_id` | int | required | Numeric spell id. |

## lorrgs trinkets

List the trinkets Lorrgs tracks on timelines.

## lorrgs spec-ranking

Fetch top-parse cooldown timelines for one spec on one encounter.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `spec_slug` | str | required | Lorrgs full spec slug, e.g. mage-frost. |
| `boss_slug` | str | required | Lorrgs boss slug, e.g. chimaerus-the-undreamt-god. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--difficulty` | str | mythic | Lorrgs difficulty slug; defaults to mythic. |
| `--metric` | str |  | Metric override, e.g. dps or hps. Defaults by spec role. |

## lorrgs spec-ranking-info

Fetch metadata for a spec ranking without the large report timeline list.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `spec_slug` | str | required | Lorrgs full spec slug, e.g. mage-frost. |
| `boss_slug` | str | required | Lorrgs boss slug, e.g. chimaerus-the-undreamt-god. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--difficulty` | str | mythic | Lorrgs difficulty slug; defaults to mythic. |
| `--metric` | str |  | Metric override, e.g. dps or hps. Defaults by spec role. |

## lorrgs comp-ranking

Fetch top composition ranking rows for an encounter.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `boss_slug` | str | required | Lorrgs boss slug, e.g. chimaerus-the-undreamt-god. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--limit` | int range | 20 | Maximum report rows to request. |
| `--role` | str (repeatable) |  | Composition role filter expression; repeatable. |
| `--spec` | str (repeatable) |  | Composition spec filter expression; repeatable. |
| `--killtime-min` | int range | 0 | Minimum kill time in seconds. |
| `--killtime-max` | int range | 0 | Maximum kill time in seconds. |

## lorrgs user-report

Fetch an already-cached Lorrgs user report overview.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `report_ref` | str | required | Warcraft Logs report URL, Lorrgs user_report URL, or report code already cached by Lorrgs. |

## lorrgs report-overview

Fetch or load a Lorrgs report overview without queueing fight/player timeline work.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `report_ref` | str | required | Warcraft Logs report URL, Lorrgs user_report URL, or report code. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--refresh / --no-refresh` | boolean | false | Ask Lorrgs to refresh overview metadata. |

## lorrgs user-report-fights

Fetch selected fights from an already-cached Lorrgs user report.

**Arguments**

| Argument | Type | Default | Description |
| --- | --- | --- | --- |
| `report_ref` | str | required | Warcraft Logs report URL, Lorrgs user_report URL, or report code already cached by Lorrgs. |

**Options**

| Option | Type | Default | Description |
| --- | --- | --- | --- |
| `--fight` | str |  | Dot-separated fight ids, e.g. 2.4.15. Defaults to fight id from URL. |
| `--player` | str |  | Optional dot-separated player source ids, e.g. 1.5.20. |
| `--type` | str |  | Optional report view type, e.g. damage-done. Defaults to type from URL. |
