name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  wheel:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          python-version: "3.12"

      - name: Resolve and install the project
        run: uv sync --frozen --extra dev --extra redis

      # The same gate as CI (lint, typecheck, import boundaries, complexity, dead code, fast
      # tests with the coverage floor), so a tag on a red commit publishes nothing.
      - name: Checks
        run: make check

      - name: Build wheel
        run: uv build --wheel

      # Attaches to the release for the pushed tag, creating it if `gh release
      # create` has not run yet.
      - name: Attach wheel to the GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.whl
          fail_on_unmatched_files: true
